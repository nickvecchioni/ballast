apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "infracost.fullname" . }}-pricing
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "infracost.labels" . | nindent 4 }}
data:
  pricing.yaml: |
    gpu_types:
    {{- range $name, $config := .Values.pricing.gpu_types }}
      {{ $name }}:
        cost_per_hour_usd: {{ $config.cost_per_hour_usd }}
    {{- end }}
